{
  "name": "express-torch",
  "version": "0.1.2",
  "license": "MIT",
  "main": "build/torch.js",
  "types": "./express-torch.d.ts",
  "repository": {
    "type": "git",
    "url": "git://github.com/JBlaak/Torch.git"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "rm -rf ./build && tsc && npm run declarations && npm run declaration-bundle",
    "pretest": "tsc --target es5 ./typings/index.d.ts ./tests/torch.test.ts",
    "test": "nyc --reporter=lcov --reporter=text --check-coverage --lines 100  --functions 100 --branches 100 mocha tests --recursive",
    "declarations": "tsc --declaration",
    "declaration-bundle": "dts-bundle --name express-torch --out ./express-torch.d.ts --main ./build/torch.d.ts && mv ./build/express-torch.d.ts ./express-torch.d.ts"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "dts-bundle": "^0.6.1",
    "mocha": "^3.1.2",
    "nyc": "^8.3.2",
    "typescript": "^2.0.6"
  },
  "dependencies": {}
}
